from labs.common.userinterface import Console
from labs.grading import Default
from ad482.common import steps # noqa


class Check(Default):

    __LAB__ = "check"

    """
    Kafka consumers exercise
    """

    def start(self):
        items = [
            # This is going to fail gracefully
            steps.check_ocp_api(fatal=False)
        ]

        Console(items).run_items(action="Starting Check")
