from ad482.common import steps
from labs.common.userinterface import Console
from labs.grading import Default as GuidedExercise


class RegenerateEnvs(GuidedExercise):
    __LAB__ = "regenerate-envs"

    """
    Deletes the Kafka cluster
    """

    def start(self):
        items = [
            steps.regenerate_env_files(),
        ]
        Console(items).run_items(action="Regenerating files for")

    def finish(self):
        steps = []
        Console(steps).run_items(action="Finishing")
