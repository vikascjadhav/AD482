"""
Server-sent events tools
"""

import json


def parse_json(raw_value: bytes):
    value = raw_value.decode("utf8").strip()
    if value:
        line = value.split(":", 1)
        if line[0] == "data" and len(line) > 1:
            return json.loads(line[1])

    return None
