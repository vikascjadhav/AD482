from labs.grading import Default as GuidedExercise
from labs.common.userinterface import Console
from ad482.common import prechecks, steps


class TroubleShootingTesting(GuidedExercise):

    __LAB__ = "troubleshooting-test"

    """
    Troubleshooting Testing Guided Exercise
    """

    def start(self):
        prechecks.verify_config()
        items = [
            steps.pull_apps_repo(),
            steps.copy_lab_app(self.__LAB__, "vehicles"),
        ]
        Console(items).run_items(action="Starting")

    def finish(self):
        items = []
        Console(items).run_items(action="Finishing")
